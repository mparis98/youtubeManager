{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}
{% block body %}

    <h1>Article</h1>
   <p>{{ article.name }}</p>
    <p>{{ article.user.firstname }}</p>
    <p>{{ article.user.lastname }}</p>
    <img src="{{ article.imageUrl }}" style="width: 50%;">
    <p>{{ article.content }}</p>

{% endblock %}